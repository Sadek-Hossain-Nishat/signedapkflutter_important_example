import 'package:flutter/material.dart';

class BackgroundandTerminated extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('BackgroundandTerminated'),
        ),
        body: Center(
          child: Text('BackgroundandTerminated'),
        ));
  }
}
