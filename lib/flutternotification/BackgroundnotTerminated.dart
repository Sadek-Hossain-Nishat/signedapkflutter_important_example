import 'package:flutter/material.dart';

class BackgroundnotTerminated extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('BackgroundnotTerminated'),
        ),
        body: Center(
          child: Text('BackgroundnotTerminated'),
        ));
  }
}
